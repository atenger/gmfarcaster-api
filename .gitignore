__pycache__/
*.pyc
.env
.env.*
.venv/
venv/

# Root-level Node tooling (scripts/sync-openapi.mjs). Per-API mcp-server/
# packages have their own nested .gitignore for their own node_modules.
node_modules/

# Claude Code
CLAUDE.md
CLAUDE.local.md
.claude/
.mcp.json

# Internal local-only docs (roadmaps, scratch notes) — never published
*.local.md
